{
  "buildCommand": "prisma generate && next build",
  "env": {
    "DATABASE_URL": "file:./businessflow.db"
  },
  "crons": [
    {
      "path": "/api/cron/send-reminders",
      "schedule": "0 * * * *"
    }
  ]
}